const gameBoard = ((doc) => {
  const board = Array.from(doc.querySelectorAll(".game-container > div"));
  return board;
})(document);

const displayController = (() => {

})();

const playerFactory = () => {

;}

console.log(gameBoard);